/**
 * RTCStatsReport Interface with missing items from typescript type definitions.
 */
export interface StatsReport extends RTCStatsReport {
    size: number;
    timestamp: number;
}
export type LogData = {
    name: string;
    timestamp: number;
    payload: string[];
};
export type LoggerCallback = (logData: LogData) => void;
/**
 * Attach a Peer Connection to periodically get updated on events and stats.
 *
 * @param pc - Peer Connection in which we attach.
 * @param logger - Logging function to log events and stats.
 * @param intervalTime - Time between each `getStats` check.
 * @param statsPreProcessor - Optional function that modifies stats.
 */
export declare const rtcStats: (pc: RTCPeerConnection, logger: LoggerCallback, intervalTime: number, statsPreProcessor?: (statsReport: Map<string, unknown>) => Promise<void>) => {
    forceStatsReport: () => void;
};
//# sourceMappingURL=rtcstats.d.ts.map