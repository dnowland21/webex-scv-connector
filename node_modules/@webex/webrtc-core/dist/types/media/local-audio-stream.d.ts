import { LocalStream } from './local-stream';
export type AppliableAudioConstraints = Pick<MediaTrackConstraints, 'autoGainControl' | 'echoCancellation' | 'noiseSuppression'>;
/**
 * An audio LocalStream.
 */
export declare class LocalAudioStream extends LocalStream {
    /**
     * Apply constraints to the stream.
     *
     * @param constraints - The constraints to apply.
     * @returns A promise which resolves when the constraints have been successfully applied.
     */
    applyConstraints(constraints?: AppliableAudioConstraints): Promise<void>;
}
//# sourceMappingURL=local-audio-stream.d.ts.map