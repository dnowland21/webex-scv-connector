import { PipelineConfig } from '../config';
import { FrameData, LadonOutput } from '../generator';
import { LadonRenderer } from './ladon-renderer';
import { WebGLRenderer } from './webgl-renderer';
export declare class BlurRenderer extends WebGLRenderer implements LadonRenderer {
    output_canvas: HTMLCanvasElement;
    readonly config: PipelineConfig;
    private program_1;
    private program_2;
    private framebuffer;
    private transfer_texture;
    private u_blur;
    private fragment_shader_common;
    private fragment_shader_pass_1_src;
    fragment_shader_pass_2_src: string;
    constructor(output_canvas: HTMLCanvasElement, config: PipelineConfig);
    render(input: FrameData, mask?: LadonOutput): Promise<void>;
    load(): Promise<void>;
    destroy(): void;
    private getBlurKernel;
}
