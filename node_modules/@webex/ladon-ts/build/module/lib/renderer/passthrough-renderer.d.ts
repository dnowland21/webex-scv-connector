import { PipelineConfig } from '../config-types';
import { FrameData } from '../generator';
import { LadonRenderer } from './ladon-renderer';
import { WebGLRenderer } from './webgl-renderer';
export declare class PassthroughRenderer extends WebGLRenderer implements LadonRenderer {
    output_canvas: HTMLCanvasElement;
    readonly config: PipelineConfig;
    private program;
    private fragment_shader;
    constructor(output_canvas: HTMLCanvasElement, config: PipelineConfig);
    render(input: FrameData): Promise<void>;
    load(): Promise<void>;
    destroy(): void;
}
