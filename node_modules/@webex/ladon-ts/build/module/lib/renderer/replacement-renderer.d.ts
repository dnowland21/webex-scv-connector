import { PipelineConfig } from '../config-types';
import { FrameData, LadonOutput } from '../generator';
import { LadonRenderer } from './ladon-renderer';
import { WebGLRenderer } from './webgl-renderer';
export declare class ReplacementRenderer extends WebGLRenderer implements LadonRenderer {
    output_canvas: HTMLCanvasElement;
    readonly config: PipelineConfig;
    private program;
    private background_canvas_ctx;
    protected background_texture: WebGLTexture;
    protected fragment_shader: string;
    constructor(output_canvas: HTMLCanvasElement, config: PipelineConfig);
    protected buildFragmentShader(): string;
    protected setupBackgroundTexture(): void;
    render(input: FrameData, mask?: LadonOutput): Promise<void>;
    load(): Promise<void>;
    destroy(): void;
}
