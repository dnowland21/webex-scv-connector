"use strict";

var _Object$defineProperty = require("@babel/runtime-corejs2/core-js/object/define-property");
_Object$defineProperty(exports, "__esModule", {
  value: true
});
exports.validateWebex = void 0;
/* eslint-disable valid-jsdoc */

// TODO: add other cases necessary for fully qualified webex instance
// eslint-disable-next-line max-len
/**
 * @param webexInstance - TODO.
 */
var validateWebex = exports.validateWebex = function validateWebex(webexInstance) {
  if (webexInstance.canAuthorize) {
    if (webexInstance.ready) {
      if (webexInstance.internal.mercury) {
        return {
          error: undefined,
          success: true
        };
      }
      return {
        error: new Error('webex.internal.mercury is not available'),
        success: false
      };
    }
    return {
      error: new Error('webex.ready is not true'),
      success: false
    };
  }
  return {
    error: new Error('webex.canAuthorize is not true'),
    success: false
  }; // TODO: change to error object
};
//# sourceMappingURL=utils.js.map
