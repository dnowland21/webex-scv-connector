import { EventMap } from 'typed-emitter';
export declare enum TrackEventNames {
    TRACK_MUTE_STATE_CHANGED = "track:mute"
}
export interface TrackMuteEvent {
    action: 'muted' | 'unmuted';
}
export interface TrackEvents extends EventMap {
    [TrackEventNames.TRACK_MUTE_STATE_CHANGED]: (event: TrackMuteEvent) => void;
}
//# sourceMappingURL=eventTypes.d.ts.map